"use client";

/** The manager's worklog: their own roster. The screen itself is shared. */

import { WorklogScreen } from "@/app/_components/WorklogScreen";

export default function ManagerWorklogPage() {
  return <WorklogScreen description="Review and filter instructor activity across your roster." />;
}
